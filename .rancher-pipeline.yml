stages:
- name: Build Temporary System
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile.temp
      buildContext: .
      tag: temp-system:latest
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
